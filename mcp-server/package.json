{
  "name": "qc-estimator-mcp",
  "version": "1.0.0",
  "private": true,
  "description": "Read-only MCP server for QC Estimator — programs, estimates, venues, pipeline",
  "scripts": {
    "dev": "tsx src/index.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "node --max-old-space-size=6144 node_modules/typescript/bin/tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "@supabase/supabase-js": "^2.45.0",
    "dotenv": "^17.4.2",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.21.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
